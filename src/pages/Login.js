import { Button } from "@material-ui/core";
import React from "react";

export default function Login() {
  return (
    <div>
      <Button variant="contained" color="primary" className="app-button">
        Login
      </Button>
    </div>
  );
}
